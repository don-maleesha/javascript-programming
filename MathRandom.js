Math.random(); //Generates a random decimal number between 0 and 1

var decimal = Math.random(); //Save the random decimal number to a variable

console.log(decimal);

var num = decimal * 10;

console.log(num);