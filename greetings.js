export const sayHi = (person) => {
  return `Hello, ${person}!`;
};

export const sayBye = (person) => {
  return `Goodbye, ${person}!`;
};

const greetings = () => {
  console.log('Hello, World!');
};

export default greetings;